interface:
  display_name: "OpenMontage"
  short_description: "Repo-specific patterns and workflows for OpenMontage"
  default_prompt: "Use the OpenMontage repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true